function sum(numOne, numTwo, numThree){
    let result = numOne + numTwo
    function subtract(num){
        let finalResult = num - numThree
        return finalResult
    }
    console.log(subtract(result))
}

sum(1,
    17,
    30
    )